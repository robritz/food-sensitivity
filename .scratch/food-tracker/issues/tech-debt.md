# Tech debt & follow-ups

**What to build:** N/A — a running list of smaller refactors, cleanups, and deferred concerns noticed while implementing other tickets. Items here aren't scoped or estimated; triage into their own ticket before picking one up.

**Status:** needs-triage

_No open items right now — everything noticed so far has been split into its own ticket: 31 (LogPage state refactor), 32 (investigate slow UI on the food log page), 33 (code-split the bundle / clear the chunk-size warning), 35 (id→name lookup maps), 36 (shared page load/state boilerplate). Add new refactors/cleanups/deferred concerns below as they come up._
