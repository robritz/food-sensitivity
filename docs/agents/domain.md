# Domain Docs Layout

**Single-context.** This repo uses one `CONTEXT.md` and one `docs/adr/` directory at the repo root — no per-package split.

- `CONTEXT.md` — the project's domain glossary, kept current by `/grill-with-docs` and `/domain-modeling`.
- `docs/adr/` — architecture decision records for hard-to-reverse decisions.
